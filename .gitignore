# System Shell Context files
.bashrc
.bash_profile
.profile
.gitconfig
.zshrc

# Local Configurations & Secrets
.env
.env.production
.env.local

# IDE environments
.idea/
.vscode/
*.code-workspace
node_modules

# Local Claude Code tooling config (agent/command definitions) — not
# application code, shouldn't be shared via the project repo.
.claude/

# backend/ is its own separate git repository
# (Fiat-Crypto-Gateway-platform-B2B-Transaction) — it was accidentally
# committed into this frontend repo too at some point. Untracked here via
# `git rm -r --cached backend`; this line stops it from being re-added.
backend/
